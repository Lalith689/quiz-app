services:
  - type: web
    name: quiz-app-backend
    runtime: java
    plan: free
    buildCommand: cd backend && mvn clean package -DskipTests
    startCommand: cd backend && java -jar target/quiz-generator-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
